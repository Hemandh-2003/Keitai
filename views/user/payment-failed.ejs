<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Failed</title>
  <link rel="stylesheet" href="/css/payment-status.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>

<body>
  <div class="payment-status-container failed">
    <div class="icon">
      <i class="fas fa-times-circle"></i>
    </div>
    <h1>Payment Failed</h1>
    <p class="error-message"><%= error || 'Your payment could not be processed' %></p>
    
    <% if (orderId) { %>
      <div class="order-info">
        <p>Order #<%= orderId %> was not completed.</p>
      </div>
    <% } %>

    <div class="action-buttons">
      <% if (orderId) { %>
        <a href="/checkout?orderId=<%= orderId %>" class="btn btn-primary">
          <i class="fas fa-credit-card"></i> Retry Payment
        </a>
      <% } %>
      <a href="/cart" class="btn btn-secondary">
        <i class="fas fa-shopping-cart"></i> View Cart
      </a>
    </div>
  </div>
</body>
</html>